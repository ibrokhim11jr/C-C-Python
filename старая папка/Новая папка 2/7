#include <stdio.h>

int main() {
    int sum = 0;
    int count = 0;
    int average;
    
    for(int i = 1; i <= 750; i++) {
        sum += i;
        count++;
    }

    average = sum / count;

    printf("Среднее арифметическое всех целых чисел от 1 до 750: %d\n", average);

    return 0;
}