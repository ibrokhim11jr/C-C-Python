#include <stdio.h>

int main() {
    int array[10] = {15, 25, 97, 100, 123, 45, 876, 33, 200, 10};

    printf("Двузначные элементы: ");
    for (int i = 0; i < 10; i++) {
        if (array[i] >= 10 && array[i] <= 99) {
            printf("%d ", array[i]);
        }
    }

    printf("\nТрехзначные элементы: ");
    for (int i = 0; i < 10; i++) {
        if (array[i] >= 100 && array[i] <= 999) {
            printf("%d ", array[i]);
        }
    }

    return 0;
}